package drai.dev.stackthecards.events;

import drai.dev.stackthecards.items.*;
import net.minecraft.block.*;
import net.minecraft.block.entity.*;
import net.minecraft.entity.player.*;
import net.minecraft.inventory.*;
import net.minecraft.item.*;
import net.minecraft.screen.*;
import net.minecraft.text.*;
import net.minecraft.util.*;
import net.minecraft.util.math.*;
import net.minecraft.world.*;
public class OpenItemMenuEvent {
//    public static TypedActionResult<ItemStack> onItemClick(PlayerEntity player, World level, Hand hand) {
        /*ItemStack handStack = player.getStackInHand(hand);
        Item item = handStack.getItem();

        if (!(item instanceof CardBinder)) {
            return TypedActionResult.pass(handStack);
        }

        EnderChestInventory enderChestContainer = player.getEnderChestInventory();
        if (enderChestContainer == null) {
            return TypedActionResult.pass(handStack);
        }


        return TypedActionResult.success(handStack);*/
//    }
}
